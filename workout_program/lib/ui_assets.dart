import 'package:flutter/material.dart';
import 'ui_navigation.dart';
import 'database_read_write.dart';
import 'globals.dart';
/*
toHubButton(context) async {
  return ElevatedButton(
      onPressed: () {
        clearSelections();
        toHub(context);
      },
      child: const Text('Home'));
}
*/
/*
toMuscleSelectButton(context) async {
  return ElevatedButton(
      onPressed: () {
        clearSelections();
        toMuscleSelection(context);
      },
      child: const Text('New Workout'));
}
*/
/*
toEquipButton(context) {
  return ElevatedButton(
    onPressed: () {
      toEquipment(context);
    },
    child: const Text('Next'),
  );
}
*/
/*
toExercisesButton(context) {
  return ElevatedButton(
    onPressed: () async {
      movementList = await getExercises(muscleList, equipList);
      toExerciseList(context);
    },
    child: const Text('Next'),
  );
}
*/
/*
saveWorkoutButton(nameController) {
  return ElevatedButton(
    onPressed: () async {
      //print("Save button pressed");
      await saveWorkout(nameController.text, movementList);
    },
    child: const Text('Save This Workout'),
  );
}
*/
/*
toSavedWorkoutsButton(context) async {
  return ElevatedButton(
      onPressed: () async {
        savedWorkoutsList = await getSavedWorkoutNames();
        toSavedWorkouts(context);
      },
      child: const Text('View Saved Workouts'));
}
*/